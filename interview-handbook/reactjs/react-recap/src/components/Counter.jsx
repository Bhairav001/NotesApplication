import React, { useEffect, useReducer, useState } from 'react'

const Counter = () => {
    // const state = useState("data")
    // console.log(state)
    const effect = useEffect(()=>{
        // console.log("now useEffect")
    },[])
    // console.log(effect)
   
  
  return (
    <div>Counter</div>
  )
}

export default Counter